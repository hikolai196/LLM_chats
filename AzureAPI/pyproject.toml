[project]
name = "azureapi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv==1.2.2",
    "requests==2.34.2",
    "streamlit==1.58.0",
]

[dependency-groups]
dev = [
    "pytest>=8.4.1",
]
